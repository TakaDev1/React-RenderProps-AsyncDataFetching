## **課題**

### 問題文

非同期で文字列データを取得し、取得結果を子コンポーネントに表示する Render Props コンポーネントを作成してください。

### 条件

(1) `DataFetcher` コンポーネントを作成
(2) `render` プロパティで取得したデータを表示
(3) `setTimeout` を使って1秒後に `"Fetched Data"` を返す
(4) TailwindCSSでボーダーとパディングを設定
(5) TypeScriptで型安全を保つ
